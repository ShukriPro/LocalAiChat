### LocalAiChat
this is local ai where u can download the model and run the ai locally without wifi.
so what you need to do? you need to generate the token from hugging face and paste that token on.

### Example :
connection.setRequestProperty(
      "Authorization",
      "Bearer hf_xxxxxxxxxxxxxx"
  )
